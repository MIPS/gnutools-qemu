/* ld_df doesn't require helper function. */
/* ldx_df doesn't require helper function. */
/* mvtg_s_df doesn't require helper function. */
/* mvtg_u_df doesn't require helper function. */
/* mvtgr_s_df doesn't require helper function. */
/* mvtgr_u_df doesn't require helper function. */
/* st_df doesn't require helper function. */
/* stx_df doesn't require helper function. */
