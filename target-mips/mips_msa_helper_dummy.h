/* copy_s_df doesn't require helper function. */
/* copy_u_df doesn't require helper function. */
/* ld_df doesn't require helper function. */
/* ldx_df doesn't require helper function. */
int64_t helper_srar_df(int64_t arg1, int64_t arg2, uint32_t df)
{
    printf("%s()\n", __func__);
    return 0; // FIXME
}


int64_t helper_srari_df(int64_t s, uint32_t m, uint32_t df)
{
    printf("%s()\n", __func__);
    return 0; // FIXME
}


int64_t helper_srlr_df(int64_t arg1, int64_t arg2, uint32_t df)
{
    printf("%s()\n", __func__);
    return 0; // FIXME
}


int64_t helper_srlri_df(int64_t s, uint32_t m, uint32_t df)
{
    printf("%s()\n", __func__);
    return 0; // FIXME
}


/* st_df doesn't require helper function. */
/* stx_df doesn't require helper function. */
